
name: tag_own_repo
on:
  push:
    paths:
      - '.github/workflows/tag_own_repo.yml'
jobs:
  old-git-container:
    runs-on: ubuntu-latest
    container: python:3.4
    steps:
      - run: git --version
